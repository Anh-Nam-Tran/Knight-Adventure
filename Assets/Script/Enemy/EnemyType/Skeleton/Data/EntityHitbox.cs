using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class EntityHitbox : MonoBehaviour
{
    /*public EntityCombat entityCombat;
    private void OnTriggerEnter2D(Collider2D other)
    {
        if (other.CompareTag("Player"))
        {
            Transform coreTransform = other.transform.Find("Core");

            if (coreTransform != null)
            {
                Transform combatTransform = coreTransform.Find("Combat");

                if (combatTransform != null)
                {
                    Combat combat = combatTransform.GetComponent<Combat>();

                    if (combat != null)
                    {
                        combat.Damage(entityCombat.DealDamage());
                        //combat.StanceDamage(entityCombat.DealStanceDamage());
                    }
                }
            }
        }
    }*/
}
